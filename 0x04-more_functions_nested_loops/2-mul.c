#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first interger to be treated
 * @b: second integer to be treated
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
